<% if user_signed_in? %>
  <div class="navbar sticky-top navbar-light bg-light px-5">
    <p>
      <%= t('.admin_panel', name: current_user.first_name) %>
      <%= link_to 'Guru', admin_tests_path, class: 'navbar-brand' %>
    </p>

    <p>
      <%= link_to t('.exit'), destroy_user_session_path, method: :delete %>
    </p>
  </div>
<% end %>
