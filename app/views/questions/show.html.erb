<p>
  <strong>Body:</strong>
  <%= @question.body %>
</p>

<%= link_to 'Edit', edit_question_path(@question) %>
<%= link_to 'Back', test_path(@question.test.id) %>
<%= link_to 'Delete', question_path(@question), 
            method: :delete, data: { confirm: 'Are you sure?' } %>
